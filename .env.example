# Demo auth works out of the box
DEMO_AUTH=true
AUTH_SECRET=solderlab-dev-secret-change-me

# Optional LLM for SolderLab Review (falls back to grounded local assistant)
# LLM_API_KEY=
# LLM_MODEL=gpt-4o-mini

# DATABASE_URL=file:./data/solderlab.json
